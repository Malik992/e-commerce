import React from "react";
import { Typography, Divider } from "@material-ui/core";

export const HomePage: React.FC<{ path: string }> = () => {
  return <p>hello home (-test-) </p>;
};

export default HomePage;
